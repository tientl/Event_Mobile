
import 'package:flutter/material.dart';

class ProfileEventPage extends StatelessWidget {
  const ProfileEventPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('Profile Event Page'),
    );
  }
}